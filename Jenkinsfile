#!groovy

node() {
    
        // cleanning workspace
        sh('rm * -rf')
        
      // if configured in scm plugin
      // checkout scm

      // if not configured scm plugin
      
      sh('git clone http://github.com/ronz1991/Medtro.git')
      // 
      writeFile file:
      // writefile files 'config', text: configfile
      
      dir('nodeapi')
      {
          sh('cat README.md')
      }
 }
